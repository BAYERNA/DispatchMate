apiVersion: 1

providers:
  - name: DispatchMate
    orgId: 1
    folder: DispatchMate
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
